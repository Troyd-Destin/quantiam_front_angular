import { MaterialLotContainer } from './material-lot-container.model';

describe('MaterialLotContainer', () => {
  it('should create an instance', () => {
    expect(new MaterialLotContainer()).toBeTruthy();
  });
});
